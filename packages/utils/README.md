# @repo/utils

Utility helpers shared across the monorepo (piece tables, parsers, byte helpers, etc.).

## Benchmarks & Tests

- Run the piece table benchmark via the test runner: `bun test tableBench`.
